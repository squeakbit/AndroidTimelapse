<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Kamera ist für diese App zwingend erforderlich (Kernfunktion ist die
         Zeitrafferaufnahme; ohne Kamera bricht CameraForegroundService sofort
         ab). required="true" sorgt dafür, dass der Play Store die App gar
         nicht erst auf Geräten ohne Kamera anbietet - required="false" wäre
         hier irreführend, da die App auf solchen Geräten nicht funktioniert. -->
    <uses-feature android:name="android.hardware.camera" android:required="true"/>
    <uses-feature android:name="android.hardware.camera.any" android:required="true"/>

    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- Nur für API 26-28 nötig (vor Scoped Storage): ohne diese Berechtigung
         schlägt das direkte Schreiben in Pictures/Timelapse/ auf diesen
         älteren Geräten fehl. Ab API 29 wirkungslos/ignoriert, da dort der
         Scoped-Storage-Pfad in PhotoCaptureHelper verwendet wird, der diese
         Berechtigung nicht braucht. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28"/>

    <!-- Lesezugriff auf Mediendateien, die NICHT von dieser App selbst über
         MediaStore erzeugt wurden (z.B. manuell/per ADB/Dateimanager zurück-
         kopierte "Ghost"-Referenzfotos). Ohne diese Berechtigung kann die App
         unter Scoped Storage (API 29+) fremde Bilder weder auflisten noch via
         ContentResolver öffnen - eigene, per MediaStore.insert() angelegte
         Fotos (PhotoCaptureHelper) sind davon nicht betroffen und funktionieren
         auch ohne diese Berechtigung. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>

    <application
        android:name=".TimelapseApplication"
        android:theme="@style/Theme.AndroidTimelapse"
        android:label="Android Timelapse"
        android:allowBackup="true"
        android:supportsRtl="true">

        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <service
            android:name=".service.CameraForegroundService"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="camera"/>

        <service
            android:name=".service.DataSyncService"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="dataSync"/>

        <receiver android:name=".AlarmReceiver" android:exported="false"/>
        <receiver android:name=".BootReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
            </intent-filter>
        </receiver>
        <receiver android:name=".ExactAlarmPermissionReceiver" android:exported="false">
            <intent-filter>
                <action android:name="android.app.action.SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED"/>
            </intent-filter>
        </receiver>
    </application>
</manifest>